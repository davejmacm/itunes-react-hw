import React from 'react';

const SongDetail = ({songs}) => {


// const position


const song = ({title})=>{
  return(
    <div className="song-detail">
      <h4>{children}</h4>
      <p>{songTitle}</p>
      </div>

})
}

export default SongDetail;
